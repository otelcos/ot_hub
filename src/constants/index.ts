/**
 * Constants barrel file.
 */

// Benchmark configurations
export {
  BENCHMARK_CATEGORIES,
  BENCHMARKS,
  TCI_CONFIG,
} from './benchmarks';
export type { BenchmarkCategory } from './benchmarks';

// Provider branding
export {
  PROVIDER_COLORS,
  PROVIDER_LOGOS,
  normalizeProviderName,
} from './providers';

// UI constants
export {
  TCI_MIN_SCORE,
  TCI_DEFAULT_MAX,
  MIN_BAR_WIDTH_PERCENT,
  TOP_RANKINGS_COUNT,
  DATE_PADDING_MS,
} from './ui';
